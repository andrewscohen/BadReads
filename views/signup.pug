extends layout.pug


block content
    if errors
        ul
            p The following error(s) occured:
            each error in errors
                li= error

    form(action="/users/signup" method="post" class="form")
        div
            h2 Sign Up For BadReads
            input(type="hidden" name="_csrf" value= csrfToken)
        div
            input(type="text" name="userName" value=user.userName placeholder="Username" class="input-bar" required)
        div
            input(type="text" name="email" value=user.email placeholder="Email" class="input-bar" required)
        div
            input(type="text" name="password" value=user.password placeholder="Password" class="input-bar" required)
        div
            input(type="text" name="confirmPassword" placeholder="Confirm Password" class="input-bar" required)
        div
            button(type="submit" class="form-submit-button") Submit
